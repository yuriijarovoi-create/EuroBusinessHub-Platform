/**
 * EuroBusinessHub API Server — scaffold
 * Replace with Express/Fastify/Hono when implementing Phase 1
 */

const PORT = process.env.PORT ?? 3001;

console.log(`EuroBusinessHub API scaffold ready on port ${PORT}`);
console.log('Modules: auth, users, companies, marketplace, transport, logistics, payments, ai, search, notifications');

export {};
