/** AI capability placeholders — re-export from shared + mock data */
export { aiCapabilities } from '@/data/aiCapabilities';

export type {
  AiArchitecture,
  AiCapability,
  AiCapabilityId,
  AiRecommendation,
  SmartSearchQuery,
  BusinessMatch,
  TransportMatch,
  LeadScore,
  CompanyRanking,
  FraudSignal,
} from '@shared/types';
